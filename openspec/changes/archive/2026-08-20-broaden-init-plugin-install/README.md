# broaden-init-plugin-install

Broaden chorus init plugin-install adapters to each agent's real install command: dsh (npm plugin CLI), openclaw (npm plugin CLI), kiro (native file-template); pi deferred
