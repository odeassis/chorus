# add-cloudfront-deploy-mode

Add a CloudFront deployment mode to install.sh and the CDK so the ACM certificate becomes optional, using CloudFront's own certificate and `*.cloudfront.net` domain by default.
