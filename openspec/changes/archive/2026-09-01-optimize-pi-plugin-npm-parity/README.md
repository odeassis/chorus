# optimize-pi-plugin-npm-parity

Productionize the Pi plugin: npm package + release flow, CC feature parity (official subagent pattern + brainstorm skill), wakeable --agent pi daemon backend, and chorus init auto-install
