# chorus-init-foundation

chorus init: interactive one-command coding-agent configuration — agent detection/selection + per-agent plugin install (native remote marketplace) + one-time credential seeding + pluggable adapter/step seam
