# Tasks

Tasks of record live in Chorus (proposal task drafts). This file mirrors them for the OpenSpec CLI.

- [ ] 1. Replace TeamCreate with harness-neutral parallel dispatch (yolo / develop / quick-dev; CC, OpenClaw, dsh)
- [ ] 2. Rewrite the reviewer wait contract in skills, manifests, hooks and dsh wake prompts (CC, Kiro, dsh) + update dsh prompt tests
- [ ] 3. Inline the spec-lite templates and remove every .chorus/specs/TEMPLATE reference (7 surfaces + docs/SPEC_LITE.md)
- [ ] 4. Add the fidelity CI guard and wire it into .github/workflows/test.yml (integration checkpoint over tasks 1-3)
